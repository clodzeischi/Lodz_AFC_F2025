export const getArea = (width: number, length: number): number => {
    return width * length;
}

export const getPerimeter = (width: number, length: number): number => {
    return (width + length) * 2;
}